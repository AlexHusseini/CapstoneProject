# === Flask ===
FLASK_SECRET_KEY=change-me-please
FLASK_DEBUG=True

# === Database ===
# By default this project uses ./instance/peer_eval.sqlite (no env needed)
# If you want to use a custom path, uncomment:
# DATABASE_URL=sqlite:///peer_eval.sqlite

# === Mail (Optional; leave blank to use in-app Dev Outbox) ===
MAIL_SERVER=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_USE_TLS=True
MAIL_DEFAULT_SENDER="KSU Peer Eval <no-reply@ksu.edu>"

# === Optional: OpenAI for Summaries/Flags ===
# If set, the system will try to use the API for better summaries and flagging.
OPENAI_API_KEY=
